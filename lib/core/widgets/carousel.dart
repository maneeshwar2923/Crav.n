import 'package:flutter/material.dart';

class CravnCarousel extends StatelessWidget {
  const CravnCarousel({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 160,
      child: Center(child: Text('Carousel placeholder')),
    );
  }
}
