import 'package:flutter/material.dart';

class Resizable extends StatelessWidget {
  final Widget child;
  const Resizable({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child; // placeholder - implement drag handles when needed
  }
}
