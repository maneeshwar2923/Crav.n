import 'package:flutter/material.dart';

class SonnerPlaceholder extends StatelessWidget {
  const SonnerPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
