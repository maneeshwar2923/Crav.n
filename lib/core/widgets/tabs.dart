import 'package:flutter/material.dart';

class CravnTabs extends StatelessWidget {
  const CravnTabs({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Tabs placeholder'));
}
