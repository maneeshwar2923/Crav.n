import 'package:flutter/material.dart';

class CravnChart extends StatelessWidget {
  const CravnChart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Chart placeholder'));
  }
}
