import 'package:flutter/material.dart';

class CravnCalendar extends StatelessWidget {
  const CravnCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Calendar placeholder'));
  }
}
