import 'package:flutter/material.dart';

class CravnTable extends StatelessWidget {
  const CravnTable({super.key});

  @override
  Widget build(BuildContext context) =>
      Center(child: Text('Table placeholder'));
}
